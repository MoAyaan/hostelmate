# HostelMate — frontend

Vite + React + Tailwind app for the MIT Bangalore HB4/HB5 roommate finder.

```
npm install
npm run dev
```

Talks to the API in `../server` (proxied to `http://localhost:8790` in dev).
For production, set `VITE_API_BASE` to the deployed backend URL — see `../DEPLOY.md`.
